<div class="container">
	<div class="row main">
		<div class="extra-border">
		<div class="col s12 m4 offset-m1 library-header z-depth-3">
				<ul class="tabs">
					<li class="tab col s2"><a href="#">Songs</a></li>
					<li class="tab col s2"><a href="#">Prior Set Lists</a></li>
				</ul>

				<div class="input-field">
					<input id="search" name="search" type="text" data-toggle="hideseek" data-list=".searchlist" placeholder="Search By Title">
				</div>
				<div class="row">
					<div class="col s12 library">
						<% if @current_org.songs.count >= 1 %>
						<ul class="collapsible searchlist" data-collapsible="accordion">
							<% @current_org.songs.order(:name).each do |song| %>
							<li>
								<div class="collapsible-header">
									<%= song.name %>
									<%= link_to(setlist_items_path(@setlist, :arrangement_id => song.arrangements.first.id), remote: true, method: "post", class: "secondary-content song-submit") do %>
									<i class="material-icons">send</i>
									<% end %> 
								</div>
								<div class="collapsible-body">
									<div class="row">
										<div class="col s6">
											<span>Arrangement: </span><br>
											<span>Key:</span><br>
											<span>Song Length: </span><br>
											<span>BPM:</span><br>
										</div>
										<div class="col s6">
											<span><%= song.arrangements.first.title %></span><br>
											<span><%= song.arrangements.first.keyof %></span><br>
											<span><%= song.arrangements.first.length %></span><br>
											<span><%= song.arrangements.first.bpm %></span><br>
										</div>
									</div>

								</div>
							</li>

							<% end %>
						</ul>
						<% else %>
						<%= link_to "New Song", new_song_path %>
						<% end %>
					</div>
				</div>	
			</div>
		</div>
		<div class="setlist-ajax">
			<%= render 'setlist' %>
		</div>
	</div>
</div>

<%= link_to "Delete Setlist", setlist_path(@setlist), method: "Delete" %>